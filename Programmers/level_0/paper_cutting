class Solution {
    public int solution(int M, int N) {
        // 프로그래머스 level_0 종이 자르기 문제
        // 규칙성을 찾다보면 답이 도출된다.
        // 세로의 길이보다 -1번 가위질을 하고
        // 가로의 가위질은 세로의 길이 * 가로의 길이 -1 을 하면 된다.
        // 세로가 분할되는 개수 == 세로의 길이 이기 때문이다.
        int answer = M -1 + M * (N - 1);
        return answer;
    }
}
